/**
 * Misao dana: Iskra's own daily lines (docs/M5-brief.md Task 3), no attribution.
 *
 * EMPTY until Pavle approves docs/QUOTES-draft.md. The approved lines go here verbatim, in the
 * draft's order. While this is empty the Home card and the detail screen do not render: a module
 * with no data is not shown (docs/HOME-V2-brief.md).
 */
export const MISLI: readonly string[] = [];
