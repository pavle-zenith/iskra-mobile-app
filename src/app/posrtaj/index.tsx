import { SlipScreen } from '@/features/posrtaj/SlipFlow';

/** The slip, first of three screens (docs/M5-brief.md, Export alignment 8). */
export default function SlipScreenRoute() {
  return <SlipScreen />;
}
