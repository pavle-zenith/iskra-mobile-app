import { SlipRecapScreen } from '@/features/posrtaj/SlipFlow';

/** The total that did not reset, and the person's reason (docs/M5-brief.md, Export alignment 8). */
export default function SlipRecapScreenRoute() {
  return <SlipRecapScreen />;
}
