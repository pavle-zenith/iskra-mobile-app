import { SlipReflectScreen } from '@/features/posrtaj/SlipFlow';

/** What led to it, with one card per trigger (docs/M5-brief.md, Export alignment 8). */
export default function SlipReflectScreenRoute() {
  return <SlipReflectScreen />;
}
