import { ModeScreen } from '@/features/poriv/ModeScreen';

/** `iskra://poriv` lands here and starts a craving if one is not already open. */
export default function PorivRoute() {
  return <ModeScreen />;
}
