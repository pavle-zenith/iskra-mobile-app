import { ConsentScreen } from '@/features/legal/ConsentScreen';

/**
 * Consent, between the welcome intro and step 1. Uncounted, like the intro: the seventeen steps
 * do not change (docs/LEGAL-brief.md, docs/WELCOME-brief.md).
 */
export default function ConsentRoute() {
  return <ConsentScreen />;
}
