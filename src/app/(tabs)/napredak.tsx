import { NapredakTab } from '@/features/ciljevi/NapredakTab';

/** Napredak, the second tab: the export's MilestoniScreen (docs/M5-brief.md). */
export default function NapredakRoute() {
  return <NapredakTab />;
}
