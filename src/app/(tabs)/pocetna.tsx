import { HomeScreen } from '@/features/home/HomeScreen';

/** Početna, the first tab. The gate at `/` decides whether someone lands here. */
export default function HomeTab() {
  return <HomeScreen />;
}
