import { QuoteScreen } from '@/features/misao/QuoteScreen';

/** Misao dana in full, opened from Home's quote slot (docs/M5-brief.md Task 3). */
export default function MisaoRoute() {
  return <QuoteScreen />;
}
