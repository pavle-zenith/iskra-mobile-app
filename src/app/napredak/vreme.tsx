import { TimeScreen } from '@/features/napredak/TimeScreen';

/** Tvoje vreme, opened from Home's timer card (docs/M4-brief.md). */
export default function TimeScreenRoute() {
  return <TimeScreen />;
}
