import { CigarettesScreen } from '@/features/napredak/CigarettesScreen';

/** Odbijene cigarete, opened from Home's cigarette card and from Ušteđevina (docs/M4-brief.md). */
export default function CigarettesScreenRoute() {
  return <CigarettesScreen />;
}
