import { RoadmapScreen } from '@/features/ciljevi/RoadmapScreen';

/** "Moj napredak", every goal on one rail, opened from Home's "Sledeći cilj" (docs/M5-brief.md). */
export default function RoadmapRoute() {
  return <RoadmapScreen />;
}
