import { MoneyScreen } from '@/features/napredak/MoneyScreen';

/** Ušteđevina, opened from Home's RSD card (docs/M4-brief.md). */
export default function MoneyScreenRoute() {
  return <MoneyScreen />;
}
