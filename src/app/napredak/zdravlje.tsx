import { HealthScreen } from '@/features/napredak/HealthScreen';

/** Zdravlje. Home's Zdravlje card that opens it arrives with M5 (docs/M4-brief.md). */
export default function HealthScreenRoute() {
  return <HealthScreen />;
}
